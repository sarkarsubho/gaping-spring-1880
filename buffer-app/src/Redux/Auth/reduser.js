
const initialState={
    isAuth:false,
    isError:false,
    token:""
}

export const reduser=(state=initialState,{type,payload})=>{
    switch(type){
        
        default:
            return state;
    }
}